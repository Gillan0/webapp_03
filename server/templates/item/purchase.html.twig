<h1>Purchase proof for {{ item.name }}</h1>
<p>Price: {{ item.price }} €</p>

{{ form_start(form, {'attr': {'enctype': 'multipart/form-data'}}) }}
    <div>
        {{ form_label(form.proof, 'Upload proof of purchase') }}
        {{ form_widget(form.proof) }}
    </div>
    <div>
        {{ form_label(form.message, 'Add a message (optional)') }}
        {{ form_widget(form.message) }}
    </div>
    <button type="submit">Upload proof</button>
{{ form_end(form) }}
